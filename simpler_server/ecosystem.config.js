//Two apps with different configurations
// TODO: This is needed when running both in the single system otherwise one app
// with multiple configurations would be fine.
//        '/Users/leelakrishnak/lucid_code_base/projects/backend_components/**/**dist**',
module.exports = {
  apps: [
    {
      name: 'start',
      script: 'yarn start',
    },
  ],
};
